{
  "scripts": {
    "dev": "bun ./index.html",
    "dev:wterm": "bun ./wterm.html",
    "build:web": "bun build --compile --target=browser --outdir=dist ./index.html",
    "build:web:wterm": "bun build --compile --target=browser --outdir=dist ./wterm.html",
    "build:cli": "bun build --compile --target=node --outfile=dist/cli ./cli.ts"
  },
  "dependencies": {
    "@wterm/dom": "^0.3.0",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0"
  }
}
