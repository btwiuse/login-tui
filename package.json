{
  "scripts": {
    "build:xterm": "bun build --target=browser --outdir=dist ./xterm.html",
    "build:wterm": "bun build --target=browser --outdir=dist ./wterm.html",
    "build:cli": "bun build --target=node --outfile=dist/cli ./cli.ts"
  },
  "dependencies": {
    "@wterm/dom": "^0.3.0",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "zustand": "^5.0.12"
  }
}
